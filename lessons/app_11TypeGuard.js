"use strict";
;
const endUser = {
    name: 'Bpb',
    email: 'as@sa.com',
    login: 'victim'
};
;
function logIdNew(id) {
    if (typeof id === 'string') {
        console.log(id);
    }
    else {
        console.log(id);
    }
}
