interface Chel {
    name: string;
};

interface Chel {
    age: number;
};

const chel: Chel = {
    name: 'Bob',
    age: 55
};

type Id = string | number;

interface ID {
    id: string | number
}
